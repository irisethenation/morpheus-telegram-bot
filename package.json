{
  "name": "morpheus-telegram-bot",
  "version": "2.0.0",
  "description": "iRise Morpheus Telegram Bot — Trust Delivery & Academy Revenue System with Automatisch Workflow Automation",
  "main": "webhook.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "automatisch:up": "docker compose up -d",
    "automatisch:down": "docker compose down",
    "automatisch:logs": "docker compose logs -f automatisch-app",
    "automatisch:setup": "node automatisch/setup-flows.js",
    "automatisch:reset": "docker compose down -v && docker compose up -d"
  },
  "dependencies": {
    "node-telegram-bot-api": "^0.64.0",
    "axios": "^1.6.2",
    "uuid": "^9.0.1"
  },
  "engines": {
    "node": ">=18.x"
  }
}
